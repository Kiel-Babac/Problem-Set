#include <iostream>
#include <conio.h>

using namespace std;
int main()
{
	int multi = 1;
	int n = 1;
	
	while(n <= 30)
	{cout << n;
	n = (n + multi);
	
	if(n >= 10)
	{multi = 2;
	}
	if(n <= 30)
	{cout << ",";
	}
	}
	
	_getch();
	return 0;
}
